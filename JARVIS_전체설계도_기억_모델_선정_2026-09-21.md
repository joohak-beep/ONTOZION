# ONTOZION — JARVIS 개인 지능 운영체제

## 전체 설계도 · 기억 계층 · 검색 · 중국 오픈웨이트 모델 선정

- 작성일: 2026-09-21
- 설계 상태: 목표 아키텍처 문서
- 인터랙티브 버전: [jarvis-integrated-design-interactive.html](./jarvis-integrated-design-interactive.html)

> 이 문서는 설계안이다. 문서에 적힌 구조가 현재 모든 장치에서 구현·검증됐다는 뜻은 아니다. 실제 구현은 각 장치와 모델에서 별도의 시험을 통과해야 한다.

## 1. ONTOZION의 목적과 한 문장 정의

ONTOZION의 목적은 **BOSS 소유형·분산형·상호학습 개인 AGI 메쉬 생태계**를 통해
BOSS의 자기결정 능력과 자유 의지를 보존하고 확장하는 것이다. 각 컴퓨터의 자비스는
서로 질문하고 반론하며, 자신의 기억·모델·도구에서 얻은 근거를 교환할 수 있다.

여기서 자유 의지는 자비스의 무제한 자율성이 아니라, BOSS가 대안을 이해하고
선택·거부·철회할 수 있는 능력이다. 질문권은 승인된 노드에 분산하지만, 질문은
실행 권한이나 헌법 수정 권한으로 승격되지 않는다.

## 1.1 공개 범위와 실행 경계

이 설계의 대표 에이전트와 운영 런타임은 **ONTOZION/JARVIS 하나**다. 외부 범용
에이전트 런타임을 설치·실행·의존성으로 사용하지 않으며, 모든 LLM·스킬·MCP·ACP·
도구·학습·검증은 JARVIS 하네스 안에서 통제한다. 한 컴퓨터의 여러 모델은 같은
JARVIS의 내부 작업자이고, 여러 컴퓨터에서는 각 JARVIS 노드가 승인된 메시지와
근거만 교환한다. 연결된 모델이나 서비스는 권한의 소유자가 될 수 없고 헌법·권한
중개기·검증기를 우회할 수 없다.

## 1.2 한 문장 정의

자비스는 단순한 음성 챗봇이 아니라, BOSS가 소유하고 자비스가 판단·계획·실행·검증·기록·학습하는 개인 지능 운영체제다.

```text
BOSS가 목적과 소유권을 가진다.
헌법이 금지선을 정한다.
자비스가 이해하고 계획한다.
권한 중개기가 실행 가능성을 판정한다.
장치 실행기가 행동한다.
검증기가 실제 결과를 확인한다.
원장이 사실을 기록한다.
학습기가 다음 실행을 더 빠르게 만든다.
```

핵심 원칙은 **병렬 사고, 단일 실행**이다. 여러 모델이 계획을 제안할 수 있지만 실제 변경은 검증과 권한 검사를 통과한 하나의 계획만 확정한다.

## 2. 전체 구조

```text
                         ┌────────────────────┐
                         │       BOSS         │
                         │ 소유자·최종 결정자 │
                         └─────────┬──────────┘
                                   │ 서명·승인
                         ┌─────────▼──────────┐
                         │  JARVIS 헌법       │
                         │ 변하지 않는 원칙   │
                         └─────────┬──────────┘
                                   │ 정책으로 변환
                         ┌─────────▼──────────┐
                         │ 정책·권한 중개기   │
                         │ 결정론적 안전 게이트│
                         └─────────┬──────────┘
                                   │ 허용된 능력만
┌──────────────┐       ┌──────────▼──────────┐       ┌──────────────┐
│ 음성·화면·폰 │──────▶│ 자비스 총괄 에이전트│◀──────│ 기억·지식 저장소│
│ 입력          │       │ 이해·계획·조정      │       │ 개인 데이터     │
└──────────────┘       └──────────┬──────────┘       └──────────────┘
                                   │ 계획
                         ┌─────────▼──────────┐
                         │ 모델 선택기        │
                         │ SLLM / 오픈웨이트  │
                         │ 클라우드 교사      │
                         └─────────┬──────────┘
                                   │ 능력 호출
                         ┌─────────▼──────────┐
                         │ Windows·macOS·Linux│
                         │ 브라우저·CLI·앱    │
                         │ 실행 장치          │
                         └─────────┬──────────┘
                                   │ 실제 결과
                         ┌─────────▼──────────┐
                         │ 검증기             │
                         │ 성공·실패·증거 확인 │
                         └─────────┬──────────┘
                                   │
                         ┌─────────▼──────────┐
                         │ 기록 장부·학습 체계 │
                         │ 재현·감사·진화      │
                         └────────────────────┘
```

### 자비스와 권한 중개기의 관계

- 자비스는 생각하고 계획하는 대표 에이전트다.
- 권한 중개기는 생각하지 않고 권한만 검사한다.
- 자비스가 중개기보다 상위 권한을 갖는 것이 아니다.
- 자비스는 중개기가 허용한 능력만 호출한다.
- 중개기는 LLM이 아니라 작고 결정론적인 프로그램이어야 한다.

## 3. 헌법 계층

### 변하지 않는 원칙

1. BOSS가 유일한 소유자다.
2. 자비스는 BOSS를 돕지만 BOSS의 가치와 소유권을 대체하지 않는다.
3. 개인 데이터는 기본적으로 로컬에 둔다.
4. 자비스는 허가받은 능력만 사용한다.
5. 되돌릴 수 없는 행동은 별도 승인한다.
6. 중요한 행동은 계획·권한·결과·증거를 기록한다.
7. 모델은 교체할 수 있어야 한다.
8. 자비스는 자기 권한을 스스로 늘릴 수 없다.
9. 헌법·소유권·상속 규칙은 자비스가 수정하지 못한다.
10. 결과를 확인하지 못하면 성공이 아니라 UNKNOWN으로 남긴다.

헌법은 긴 프롬프트에만 두지 않는다. 최소 권한, 승인, 만료, 허용 경로, 긴급 정지 같은 항목은 코드와 운영체제 규칙으로도 고정한다.

## 4. 하네스 구성

| 구성요소 | 역할 | 경계 |
|---|---|---|
| 입력·세션 관리자 | 음성·문자·화면을 세션으로 묶음 | 사용자의 의도를 임의로 바꾸지 않음 |
| 기억 관리자 | 필요한 개인 기억과 과거 작업을 찾음 | 원본 비밀정보를 무제한 전달하지 않음 |
| 모델 선택기 | 작업 난이도·개인정보 등급에 맞는 모델 선택 | 모델이 직접 외부 행동을 하지 못하게 함 |
| 계획기 | 목표를 단계와 조건으로 분해 | 권한을 스스로 부여하지 않음 |
| 권한 중개기 | 능력·대상·범위·만료·승인 검사 | 새로운 목표를 만들지 않음 |
| 실행기 | 브라우저·CLI·API·앱에 행동 전달 | 임의 관리자 셸을 실행하지 않음 |
| 검증기 | 화면·파일·상태·응답을 다시 확인 | 출력 문구만 보고 성공 처리하지 않음 |
| 기록·학습기 | 실행 원장·스킬·시험·모델 버전 저장 | 운영 중 기본 모델을 몰래 바꾸지 않음 |

### 4.1 온톨로지 하네스 — 자비스 OS의 의미 커널

하네스를 권한 중개기나 도구 모음으로만 보면 자비스 OS의 핵심을 놓친다.
하네스 전체는 **온톨로지 커널 + 기억 투영층 + 실행 계약 + 검증·기록 버스**다.
온톨로지는 단순한 분류표가 아니라, “무엇이 무엇과 어떤 관계를 맺고, 언제,
어떤 근거와 권한으로 유효한가”를 기계가 질의할 수 있게 만드는 공통 의미
계약이다.

#### 자비스 OS 안에서의 계층

| 계층 | 역할 | 온톨로지와의 관계 |
|---|---|---|
| 자비스 총괄 에이전트 | 자연어 이해·계획·조정·보고 | 온톨로지 상황 프레임을 만들고 질의함 |
| 온톨로지 커널 | 객체·관계·시간·상태·근거의 기준 | 모든 기억층과 실행기가 공유하는 의미 원장 |
| 기억 관리자 | 필요한 맥락 회수·승격·정정 | 그래프·키워드·벡터 색인은 원장의 투영 |
| 권한 중개기 | capability·대상·범위·만료·승인 검사 | 온톨로지 관계가 불명확하면 거부 |
| 실행기·장치 에이전트 | 브라우저·CLI·API·앱 동작 | 선언된 capability만 수행하고 사건을 보고 |
| 검증기·기록기 | 실제 결과·증거·회귀·학습 저장 | 실행 전후 온톨로지 상태를 비교 |

자비스가 중개기보다 상위 권한을 갖는 구조가 아니다. **하네스가 OS 전체**이고,
총괄 에이전트는 그 위에서 계획하는 대표 프로세스이며, 온톨로지 커널과 권한
중개기는 모델보다 아래에서 의미와 실행을 강제하는 결정론적 계층이다.

#### 핵심 온톨로지 객체와 관계

| 객체 | 대표 관계 | 하네스에서의 기능 |
|---|---|---|
| `Person / BOSS / FamilyMember` | `owns`, `governs`, `inherits_from`, `consents` | 소유자·후계자·동의 범위를 분리한다. 지식 상속은 실행 권한 상속이 아니다. |
| `Agent / Model / Tool / Device` | `runs_on`, `can_invoke`, `observes`, `reports_to` | 어떤 모델이 어떤 장치의 어떤 도구를 호출했는지 추적한다. |
| `Task / Skill / Event / Outcome` | `contains`, `derived_from`, `caused`, `verified_as` | 음성 요청을 실행 그래프와 검증된 결과에 연결한다. |
| `Capability / Policy / Approval` | `authorized_by`, `targets`, `scoped_to`, `expires_at` | 권한의 범위·대상·만료·승인을 결정한다. |
| `MemoryItem / Evidence / Source` | `asserts`, `supports`, `contradicts`, `supersedes` | 기억을 덮어쓰지 않고 출처·충돌·최신성을 보존한다. |

#### 모든 의미 기억층의 온톨로지 계약

모든 기억은 최소한 다음 필드를 공유해야 한다.

~~~text
stable_id · subject · predicate · object
valid_from · valid_to · created_at · source_ref
confidence · sensitivity · owner_id · consent_ref
evidence_ref · contradiction_refs · schema_version
~~~

| 기억층 | 온톨로지 프레임 | 색인·보존 |
|---|---|---|
| 단기 기억 | 현재 BOSS·세션·목표·대상·대기 승인·도구 상태 | RAM 키·정확한 상태 조회·세션 만료 |
| 중기 기억 | 프로젝트·열린 작업·결정·실패·반복 절차 | 시간·프로젝트·task ID·관계 그래프·TTL |
| 장기 기억 | 안정된 선호·사람·장치·절차·가치관 | 그래프·벡터·키워드 병렬 검색·승격 검토 |
| 사실 원장 | 원자적 주장·근거·반박·유효기간·상속 정책 | `predicate`·`as-of`·`provenance` 조회·정정 전파 |

따라서 벡터 데이터베이스는 기억의 진실 원장이 아니다. 벡터·BM25·그래프는
동일한 정본 사건 원장에서 다시 만들 수 있는 검색 투영이다. 색인이 망가져도
원장에서 복구할 수 있어야 하며, 모순은 삭제하지 않고 별도 관계로 남겨야 한다.

#### 자연어에서 실행까지의 온톨로지 흐름

~~~text
자연어
  → 엔티티·관계 정규화
  → 상황 프레임(주체·목표·대상·시간·상태·권한)
  → 의미 기억 + 정확 상태 + 근거 병렬 조회
  → 헌법·권한 질의
  → 계획 + capability 토큰
  → 장치 실행
  → 관찰 사건·결과·증거를 원장에 기록
  → 그래프·벡터·키워드 투영 비동기 갱신
  → 음성·화면 보고
~~~

#### 성능을 잃지 않는 구현

온톨로지화를 위해 매번 큰 LLM을 호출하지 않는다.

1. 정본은 append-only 사건 원장으로 두고, 안정된 ID와 타입을 먼저 확인한다.
2. 현재 세션과 자주 쓰는 관계는 RAM 캐시와 컴파일된 정책 질의로 처리한다.
3. 엔티티 연결·시간 정규화·권한 판정은 결정론적 코드로 우선 처리한다.
4. 벡터 임베딩·중복 제거·그래프 투영은 실행 경로 밖에서 비동기 처리한다.
5. 단순 반복 작업은 온톨로지로 검증된 `skill_id`를 바로 호출한다.

#### 온톨로지 합격 기준

- 모든 의미 기억 쓰기에 안정된 ID·관계·출처·시간이 있다.
- 모든 색인은 정본 원장에서 삭제 후 재생성할 수 있다.
- 사실 충돌을 조용히 덮어쓰지 않고 `contradicts`로 보존한다.
- capability 관계가 없거나 만료되면 도구를 실행하지 않는다.
- `forget/correct/wipe`가 원장과 모든 투영에 전파된다.
- 가족 상속 모드는 지식 읽기 범위를 넓힐 수 있지만 실행 권한을 자동으로 넓히지 않는다.

### 4.2 CodePlan과 로컬 모델 분업

LLM이 사람용 문장을 출력하고, 다시 그 문장을 코드가 파싱하는 이중 변환은
느리고 불안정하다. JARVIS에서는 **로컬 모델이 CodePlan을 만드는 설계자**이고,
**하네스가 CodePlan을 검증·컴파일·실행하는 실행자**다. 코드 자체는 사람이
읽는 소스 텍스트로 남지만, 어떤 파일을 어떤 이유로 어떤 테스트와 함께
바꿀지는 타입이 있는 계획으로만 전달한다.

#### 세 가지 모델 통로

| 통로 | 담당 | 호출 조건 |
|---|---|---|
| 반사 통로 | 소형 로컬 SLLM: 의도 분류·엔티티 연결·검증된 `skill_id` 선택 | 반복 작업·단순 수정·낮은 위험 |
| 설계 통로 | 강한 로컬 오픈 웨이트: 복잡한 설계·리팩터링·테스트 계획 | 새 기능·다중 파일·높은 추론 필요 |
| 교사 통로 | 선택적 클라우드 모델: 비식별화된 계획 비평·어려운 사례 보완 | 로컬 확신도 낮음·스키마 실패·테스트 반복 실패·새 아키텍처 |

클라우드 모델은 직접 셸·파일·관리자 권한을 갖지 않는다. 민감한 소스와
기억은 기본적으로 로컬에 남고, 교사 호출이 필요하면 최소 문맥과 비식별
CodePlan만 보낸다. 오픈 웨이트 모델은 “공짜”가 아니라 모델·토크나이저·
데이터셋·양자화 라이선스와 실제 하드웨어 비용까지 측정해야 한다.

#### CodePlan 최소 계약

~~~json
{
  "kind": "code_plan",
  "request_id": "uuid",
  "repository": "jarvis",
  "branch": "develop",
  "files": [
    {"path": "src/router.py", "symbols": ["route_request"], "operation": "patch"}
  ],
  "tests": ["python -m compileall src", "tests/test_router.py"],
  "constraints": ["no_constitution_change", "no_admin_shell"],
  "capability": "repo.patch",
  "approval": "policy_scoped"
}
~~~

#### 하네스 컴파일·실행 단계

1. CodePlan의 JSON Schema와 버전을 검사한다.
2. 저장소·브랜치·파일·심볼이 온톨로지에 존재하는지 확인한다.
3. 헌법·위험 등급·capability·승인·만료를 결정론적으로 검사한다.
4. 허용된 `repo.search/read/patch/test/diff/rollback` 도구로만 실행한다.
5. 같은 파일의 쓰기는 직렬화하고, 독립 검색·읽기·시험은 병렬화한다.
6. 컴파일·린트·단위시험·회귀·실제 post-condition을 확인한다.
7. 실패하면 전체 코드를 다시 생성하지 않고 실패한 계획 단계만 재계획한다.

#### 속도와 안정성 지표

`CodePlan schema pass rate`, `patch apply success`, `first-pass compile`,
`targeted-test pass`, `full-regression pass`, `verified change p95`,
`replan count`, `tokens per verified change`를 기록한다. 구조화된 출력은
형식 오류를 줄여 주지만 코드의 논리적 정확성을 보장하지 않으므로,
최종 합격은 항상 실행 후 검증과 diff로 판정한다.

## 5. 요청의 생애

```text
1. BOSS가 말한다.
2. 호출어·사용자·세션을 확인한다.
3. 음성을 글로 변환한다.
4. 현재 대화와 필요한 기억을 회수한다.
5. 대화·정보·로컬 작업·외부 행동·위험 행동으로 분류한다.
6. 자비스가 실행 계획을 만든다.
7. 권한 중개기가 계획을 검사한다.
8. 허용된 능력 토큰을 발급한다.
9. 장치 에이전트가 실행한다.
10. 화면·상태·응답을 다시 확인한다.
11. VERIFIED·FAILED·BLOCKED·UNKNOWN을 판정한다.
12. 음성과 화면으로 결과를 보고한다.
13. 실행 기록과 학습 자료를 저장한다.
```

읽기와 준비는 병렬로 처리할 수 있지만, 실제 변경·결제·발송은 하나의 확정 단계로 묶는다.

## 6. 실행 속도를 높이는 구조

### 세 가지 실행 통로

#### 즉시 실행 통로

반복 작업은 SLLM이 의도만 파악하고 저장된 `skill_id`를 호출한다. 클라우드 모델을 다시 부르지 않는다.

```text
자연어 → skill_id → 사전 검증된 실행 그래프 → 권한 확인 → 실행
```

#### 계획 통로

새로운 작업은 오픈웨이트 모델이나 클라우드 교사로 계획을 만들고, 성공한 절차를 다음부터 스킬로 저장한다.

#### 승인 통로

결제·삭제·거래·외부 발송은 자비스가 준비까지 자동으로 수행하고, 마지막 확정만 BOSS가 한다.

### 속도 원칙

- 대표 에이전트 하나가 지휘하고 전문 실행기는 병렬로 움직인다.
- 모델·브라우저 세션·API 연결·자주 쓰는 기억은 미리 준비한다.
- 공식 API를 화면 클릭보다 먼저 사용한다.
- DOM·접근성 트리를 화면 인식보다 먼저 사용한다.
- 임베딩·중복 검사·색인 갱신은 답변을 막지 않는 백그라운드 작업으로 처리한다.
- 같은 작업의 재시도는 멱등 키(중복 실행 방지용 고유값)로 보호한다.
- 성공한 작업은 다음 실행의 고속 경로인 스킬로 컴파일한다.

## 7. 기억 계층

### 단기 기억

현재 음성 대화, 진행 중 계획, 최근 화면, 미완료 승인, 방금 실행한 명령을 저장한다.

- RAM 또는 빠른 로컬 저장소
- 수분~수시간 유지
- 순서·세션 ID·작업 ID로 직접 조회
- 임베딩 검색보다 정확한 현재 상태를 우선

### 중기 기억

최근 며칠~몇 주의 프로젝트, 결정, 오류, 반복 절차를 저장한다.

- 날짜·프로젝트·사람·작업 ID로 검색
- 키워드 검색과 의미 검색을 함께 사용
- 새 정보가 기존 정보와 다르면 이전 기록을 삭제하지 않고 `superseded`로 표시

### 장기 기억

BOSS의 취향, 원칙, 검증된 사실, 완성된 스킬, 장기 프로젝트 지식을 저장한다.

- 자동 덮어쓰기 금지
- 출처·확인 시각·유효기간 보존
- 충돌한 정보는 바로 교체하지 않고 보류 또는 충돌 상태로 저장

### 사실 원장

기억과 별도로 실제 승인·실행·결과·증거를 추가만 가능한 장부에 저장한다. 기억이 “보냈다”고 말해도 실제 발송 여부는 원장에서 확인한다.

## 8. 기억 색인과 검색

### 여러 색인을 병렬로 사용

| 색인 | 잘 찾는 것 |
|---|---|
| 전문 검색(BM25) | 파일명·명령어·숫자·정확한 문장 |
| 의미 검색(벡터) | 표현은 달라도 뜻이 같은 내용 |
| 시간 색인 | 최근 사건·특정 날짜의 결정 |
| 관계망 | 사람·프로젝트·파일·도구 연결 |
| 절차 색인 | 반복 스킬·실행 그래프 |
| 출처·권한 색인 | 근거 위치와 볼 수 있는 모델 |

### 검색 흐름

```text
요청 유형 분류
 → 필요한 색인 선택
 → 전문·벡터·시간·관계·출처 병렬 조회
 → 권한·유효기간 필터
 → 후보 30~50개 재순위
 → 최종 3~8개만 문맥에 넣기
```

초기 재순위 점수는 다음 요소를 합친다.

```text
의미 유사도
+ 정확한 단어 일치
+ 시간적 최신성
+ 중요도
+ 출처 신뢰도
- 오래된 정보 감점
- 충돌 정보 감점
```

이 가중치는 시작점이며 실제 자비스 작업으로 조정한다.

### 기억 기록의 최소 구조

```json
{
  "memory_id": "mem_001",
  "tier": "medium",
  "type": "procedure",
  "content": "보고서 작성 절차",
  "entities": ["보고서", "투자실험실"],
  "valid_from": "2026-09-01",
  "valid_to": null,
  "confidence": 0.91,
  "source_refs": ["event_183", "screen_44"],
  "privacy_class": "D1",
  "supersedes": null,
  "status": "active"
}
```

## 9. 개인정보 등급

| 등급 | 예시 | 기본 처리 |
|---|---|---|
| D0 공개 | 공개 문서·공개 웹 | 필요하면 클라우드 사용 |
| D1 개인 | 개인 일정·취향·일반 프로젝트 | 로컬 우선, 필요한 부분만 축약 전송 |
| D2 민감 | 가족·재무·비공개 사업자료 | 로컬 처리 우선, 외부 전송은 별도 승인 |
| D3 비밀 | 비밀번호·카드번호·API 키 | 모델과 클라우드에 원문 전달 금지 |

## 10. 모델 구성

### 개인 SLLM

BOSS의 말투, 습관, 개인 기억, 반복 스킬, 빠른 의도 분류를 담당한다.

### 오픈웨이트 모델

낯선 문제, 복잡한 계획, 코드 분석, 넓은 지식 검색을 담당한다.

### 클라우드 모델

민감정보를 제거한 어려운 사례의 교사·비평가·시험 생성기로만 사용한다. 직접 도구를 호출하거나 로컬 장치를 조작하지 않는다.

```text
SLLM 제안 ─────────┐
                   ├─ 검증기·권한 중개기 → 단일 실행
오픈웨이트 제안 ───┘
```

## 11. 중국 오픈웨이트 모델 선정 기준

### 원칙

“검열되지 않은 모델”이라는 이름을 그대로 믿지 않는다. 공식 가중치와 모델 해시를 확인하고, 실제 자비스 시험으로 판단한다.

### 역할별 후보

- **빠른 개인 비서 후보:** Qwen3 계열
- **깊은 추론·코딩 후보:** DeepSeek-R1 계열 또는 R1 증류 모델
- **개인화 후보:** BOSS 데이터로 별도 학습한 SLLM

Qwen3와 DeepSeek-R1의 공식 보고서는 추론·코딩·수학 성능을 보여주지만, 이런 결과가 곧 사실성·정치적 중립성·검열 행동을 증명하는 것은 아니다. [Qwen3 기술 보고서](https://arxiv.org/abs/2505.09388) · [DeepSeek-R1 논문](https://arxiv.org/abs/2501.12948)

### 정직성 시험

1. 허용된 질문을 불필요하게 거부하지 않는가
2. 한국어·영어·중국어에서 근거 없이 결론이 뒤집히지 않는가
3. 모르는 정보와 추정을 구분하는가
4. 검색·계산·공식 자료와 대조했을 때 숫자를 만들지 않는가
5. 실행하지 않은 일을 완료했다고 보고하지 않는가
6. 로컬 가중치와 API 경로의 응답이 다르게 정책화되지 않는가

DeepSeek에 대한 한 독립 연구는 정치적으로 민감한 질문에서 정보가 최종 답변에서 생략·완화되는 현상을 관찰했다. 또 다른 연구는 중국어와 영어에서 정치적 답변이 달라지는 언어 편향을 보고했다. 이는 모든 중국 모델이 동일하다는 뜻은 아니지만, 모델별 시험이 필요한 근거다. [정보 억제 감사 연구](https://arxiv.org/abs/2506.12349) · [다국어 편향 연구](https://arxiv.org/abs/2602.06371)

## 12. 양자화 정책

“양자화 후 성능이 전혀 줄지 않는 모델”은 미리 보장할 수 없다. 원본과 양자화본을 자비스 업무로 비교해 성능 저하가 허용 범위 안인 버전만 채택한다.

```text
BF16·FP16 원본 기준
 → FP8·INT8
 → Q8
 → Q6
 → AWQ·GPTQ 4-bit
```

AWQ는 중요한 활성화 채널을 보호해 저비트 양자화 오차를 줄이는 방식이며, GPTQ는 낮은 비트에서 정확도 보존을 목표로 하는 후처리 양자화 방식이다. 하지만 모델·하드웨어·작업마다 결과가 달라서 실측이 필요하다. [AWQ 연구](https://arxiv.org/abs/2306.00978) · [GPTQ 연구](https://arxiv.org/abs/2210.17323)

### 양자화 회귀시험

| 항목 | 비교할 것 |
|---|---|
| 대화 | 한국어 자연스러움·문맥 유지 |
| 도구 호출 | JSON 형식·명령 정확도 |
| 코드 | 실제 테스트 통과율 |
| 기억 | 검색한 사실의 정확성 |
| 사실성 | 근거 없는 주장 비율 |
| 거부 행동 | 허용된 질문의 부당한 거부 |
| 속도 | 첫 응답·전체 완료 시간 |
| 안정성 | 긴 대화의 반복·망각 |

초기 승격 기준은 설계 목표로 다음처럼 둘 수 있다.

```text
대화 성공률 변화       <= 2%
도구 호출 성공률 변화   <= 1%
코드 테스트 통과율 변화 <= 1%
사실성 저하             없음
부당한 거부 증가         없음
P95 지연시간             원본보다 개선
```

## 13. 권한 설계

### 녹색 · 즉시 처리

- 파일·웹 읽기
- 일정 조회
- 로컬 검색
- 되돌릴 수 있는 초안 작성

### 노란색 · 조건부 처리

- 브라우저 입력
- 파일 이름 변경
- 로그인된 서비스의 일반 작업
- 미리 승인된 반복 업무

### 빨간색 · BOSS 확인

- 외부 메일·게시물 발송
- 결제·예약·증권 거래
- 대량 삭제
- 관리자 권한 변경
- 헌법·권한·모델 변경

자비스에 관리자 비밀번호를 주지 않는다. 자비스는 `backup_project` 같은 제한된 능력을 요청하고, 중개기가 범위·만료·대상을 확인한 뒤 서명된 절차만 실행한다.

### 13.1 자비스의 선제적 생활·금융 정리

자비스는 BOSS가 먼저 시키기를 기다리는 비서가 아니라, 로컬 자료를 살펴보고
귀찮고 반복적인 일을 먼저 찾아 **발견 → 제안 → 준비 → 제한된 실행 → 결과 확인**의
순서로 처리하는 개인 비서다. 다만 발견과 제안은 넓게 자동화하고, 돈을 움직이거나
권리를 없애는 실행은 정책과 짧은 권한으로 제한한다.

| 작업 | 자비스가 먼저 할 수 있는 일 | 실행 경계 |
|---|---|---|
| 구독 | 30일 이상 미사용, 비용, 가족 공유, 해지 수수료를 찾아 해지 후보를 제안하고 해지 화면을 준비 | 수수료 없음·혜택 손실 없음·BOSS의 사전 규칙일 때만 자동 해지 |
| 쿠폰·포인트 | 만료일, 사용 조건, 현금·상품 전환 가능성을 찾아 알림·전환 후보를 준비 | 제3자 전송·전환 손실·유효기간 변경은 BOSS 확인 |
| 은행 계좌 | 거의 쓰지 않는 계좌, 자동이체, 잔액, 연결 서비스를 점검하고 정리 순서를 제안 | 계좌 폐쇄·이체·출금은 항상 현재 거래 내역과 BOSS 확인 |
| 투자 | 최신 데이터의 시각과 근거를 붙여 후보·위험·대안을 제안 | 매수·매도는 명시된 전략·금액·손실 한도와 별도 주문 확인 없이는 실행하지 않음 |

“좋은 상황이면 투자한다”는 자연어만으로는 실행 규칙이 아니다. 자산 범위,
최대 금액, 하루 한도, 손실 한도, 레버리지 금지, 유효기간, 중지 조건을 기계가
검사할 수 있는 정책으로 먼저 만든다. 자비스는 후보를 먼저 알릴 수 있지만,
정책에 없는 판단을 새 권한으로 바꾸지 않는다. 금융기관의 공식 API와 읽기 전용
연결을 우선하고, 브라우저 자동화는 API가 없는 준비 작업에 한정한다.

### 13.2 패스키와 자격증명 중개기

패스키는 암호화한 비밀번호 파일이 아니라 공개키·비밀키로 로그인하는 방식이다.
비밀키는 Windows Hello·TPM, macOS Secure Enclave, 또는 FIDO 보안키가 사용하고,
LLM에는 원문 비밀번호·비밀키를 주지 않는다. 자비스가 보는 것은
`account_ref=approved_bank_main` 같은 별명과 제한된 요청뿐이다.

1. 자비스가 `login(account_ref)`를 요청한다.
2. 자격증명 중개기가 대상·사이트·유효시간을 확인한다.
3. 운영체제 인증기가 BOSS의 PIN·생체 확인 또는 보안키로 로그인 요청에 서명한다.
4. 중개기는 `read_only`, `subscription_cancel`, `order_draft`처럼 짧고 구체적인 능력만 발급한다.
5. 결과·영수증·실패 이유를 원장에 남기되 비밀번호·패스키·카드번호는 기록하지 않는다.

사이트가 패스키를 지원하지 않으면 비밀번호를 로컬 자격증명 저장소에서 일시적으로
주입할 수 있지만, 프롬프트·음성 기록·일반 로그에는 남기지 않는다. 이미 로그인된
브라우저 세션도 무제한 권한으로 보지 않으며, 금융 화면에는 조회·초안·주문 실행을
서로 다른 도구로 분리한다. CAPTCHA·2차 인증·신원 확인은 우회하지 않고 BOSS에게
넘긴다. 엄격한 로컬 전용이 필요하면 동기화하지 않는 기기 귀속 패스키를 선택한다.

## 14. 실행 상태와 기록

```text
PROPOSED
 → POLICY_CHECKED
 → PREPARED
 → APPROVED 또는 GRANTED
 → EXECUTING
 → OBSERVED
 → VERIFIED
 → COMMITTED
```

문제가 생기면 다음 상태로 남긴다.

- `FAILED`: 실패 원인을 확인함
- `BLOCKED`: 정책상 실행하지 않음
- `UNKNOWN`: 실제 결과를 확인하지 못함
- `RECOVERED`: 복구 후 다시 확인함

### 실행 기록 예시

```json
{
  "event_id": "evt_20260921_000184",
  "task_id": "task_20260921_0042",
  "actor": "jarvis",
  "constitution_version": "1.0.0",
  "policy_version": "2026-09-21.3",
  "model_version": "sllm-0.8.2",
  "capability": "browser_read",
  "approval_id": null,
  "plan_hash": "sha256:...",
  "evidence": ["screenshot_ref", "http_status"],
  "status": "verified"
}
```

### 14.1 변경 이력과 선택적 블록체인 봉인

헌법·정책·스킬은 필요할 때 BOSS의 승인으로 새 버전으로 바꿀 수 있다. 다만
이전 버전을 지우지 않고, 변경 전후의 차이·이유·승인자·적용 시각을 추가 전용
원장에 남긴다. 블록체인은 실행 권한을 주는 곳이 아니라 이 원장이 나중에
몰래 바뀌지 않았음을 확인하는 선택적 외부 증인이다.

```text
작업 실행 → 로컬 암호화 원장 기록 → BOSS 서명·버전 지문 생성
          → 봉인 큐에 추가 → 백그라운드에서 Merkle Root를 외부 체인에 앵커링
```

- 실시간 음성·브라우저·CLI·금융 주문은 블록체인 확인을 기다리지 않는다.
- 원문 음성, 임베딩, 비밀번호, API 키, 화면 캡처, 개인 기억은 체인에 기록하지 않는다.
- 체인이 중단되어도 작업은 계속하고, 봉인 큐는 연결 복구 뒤 재처리한다.
- 한 대의 BOSS 소유 컴퓨터에서는 서명된 append-only 원장과 암호화 백업만으로 충분할 수 있다.
- 여러 소유자의 자비스 협업이나 가족 상속 증명이 필요할 때만 외부 체인 앵커를 사용한다.

#### 블록체인 적용 분야 판단표

| 분야 | 기록 묶음 | 외부 봉인이 유용한 이유 |
|---|---|---|
| 금융·증권 | 주문 계획·승인·체결 결과 지문 | 사후 거래 감사와 분쟁 대비 |
| 헌법·상속 | JARVIS 버전·정책 변경·승인 시각 | 상속될 기준 버전의 존재 증명 |
| 모델·스킬 공급망 | 모델 해시·시험 결과·배포 버전 | 실제 실행 모델의 출처 확인 |
| 다중 노드 | 노드 서명·질문·답변·결과 지문 | 서로 다른 컴퓨터의 기록 대조 |
| 동의·권한 | 동의 버전·철회 시각·capability 범위 | 허용 시점과 범위 확인 |

판단 기준은 기록의 중요성만이 아니라, 독립된 주체가 같은 기록을 확인해야
하는지와 변경 시 손실·분쟁이 발생하는지다. 개인 기억·음성·임베딩·비밀번호·
원본 금융자료는 체인에 올리지 않으며, 법률상 공증이나 금융기관의 공식 기록을
블록체인이 대신하지 않는다.

비밀번호·카드번호·API 키·원본 비밀 음성은 기록하지 않는다.

## 15. 실패와 복구

- 네트워크가 끊기면 외부 행동을 보류한다.
- 결과를 확인하지 못하면 성공 처리하지 않는다.
- 재시도에는 멱등 키를 사용한다.
- 서비스가 재시작되면 원장에서 마지막 확인 단계를 읽는다.
- 클라우드 장애 시 로컬 모델로 전환하되 허용된 작업만 수행한다.
- 권한 중개기가 이상하면 위험 작업을 모두 막는다.
- BOSS의 긴급 정지로 모든 실행 토큰을 폐기한다.

## 16. 구현 순서

1. 헌법·BOSS·권한 중개기·추가 전용 실행 원장
2. 자연스러운 음성 세션과 단기 기억
3. 중기·장기 기억과 병렬 색인
4. 브라우저·파일·CLI·API 능력 등록부
5. 실제 결과 검증과 복구
6. SLLM·오픈웨이트·클라우드 교사 라우팅
7. 스킬 승격과 회귀시험
8. 상속·키 교체·다중 장치 운영

## 17. 완료 판정

### 대화 게이트

음성이 끊기지 않고, 호출 세션이 자연스럽게 유지되는가?

### 권한 게이트

모델이 관리자 권한을 직접 얻거나 범위를 넓힐 수 없는가?

### 실행 게이트

말한 작업이 실제 장치에서 수행되고 결과가 확인되는가?

### 기억 게이트

필요한 기억을 빠르게 찾고, 오래된 정보와 현재 사실을 구분하는가?

### 모델 게이트

양자화 후 대화·코드·도구·기억 검색 성능이 허용 범위 안인가?

### 학습 게이트

성공한 작업이 스킬·시험·기억으로 재사용되는가?

## 18. 최종 원칙

> 자비스는 모델을 믿는 시스템이 아니라, 모델이 틀릴 수 있다는 전제로도 안전하게 실행되는 시스템이어야 한다.

> 처음에는 자비스가 생각하고, 다음부터는 성공한 절차가 자비스를 대신해 빠르게 실행한다.
